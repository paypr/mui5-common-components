import PlayArrow from '@mui/icons-material/PlayArrow';
import { SvgIconProps } from '@mui/material/SvgIcon';
import React from 'react';

const PlayIcon = (props: SvgIconProps) => <PlayArrow {...props} />;
export default PlayIcon;
