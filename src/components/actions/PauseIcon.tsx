import Pause from '@mui/icons-material/Pause';
import { SvgIconProps } from '@mui/material/SvgIcon';
import React from 'react';

const PauseIcon = (props: SvgIconProps) => <Pause {...props} />;
export default PauseIcon;
