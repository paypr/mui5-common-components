import Sync from '@mui/icons-material/Sync';
import { SvgIconProps } from '@mui/material/SvgIcon';
import React from 'react';

const RefreshIcon = (props: SvgIconProps) => <Sync {...props} />;
export default RefreshIcon;
